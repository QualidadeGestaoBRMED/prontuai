# Requirements minimalista para usar APENAS Textract (sem Docling)
# Use este arquivo se você definir USE_TEXTRACT=true no .env
#
# Build Docker com este arquivo:
# docker build --build-arg REQUIREMENTS_FILE=requirements-textract-only.txt -t prontuai-backend .

# FAISS CPU (usado em FAQ e validação de exames)
faiss-cpu==1.8.0

# Web Framework
fastapi>=0.115.0
uvicorn>=0.30.0
gunicorn>=21.0.0
pydantic>=2.0.0
python-multipart>=0.0.6

# AI/ML
openai>=1.0.0
numpy==1.26.4

# Data Processing
pandas>=2.0.0

# Utilities
python-dotenv>=1.0.0
playwright>=1.43.0
tenacity>=8.2.0

# AWS SDK (Textract)
boto3>=1.34.0
PyPDF2>=3.0.0

# Dependências de Teste
pytest>=7.0.0
pytest-asyncio>=0.20.0
httpx>=0.23.0
